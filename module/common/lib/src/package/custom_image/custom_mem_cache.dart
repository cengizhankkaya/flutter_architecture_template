/// A class that holds the memory cache dimensions for images.
final class CustomMemCache {
  /// Creates a [CustomMemCache] with the specified height and width.
  const CustomMemCache({required this.height, required this.width});

  /// memory cache height.
  final int height;

  /// memory cache width.
  final int width;
}
